# 安全策略

## 支持范围

安全修复仅面向 `main` 分支的最新版本。更新前请保留自己的素材包和本地凭据文件；不要把凭据文件复制进本仓库。

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告安全问题，并说明：

- 受影响的 Skill、脚本与版本或提交；
- 可重复的最小步骤；
- 预期行为和实际行为；
- 已脱敏的错误码或日志；
- 你认为可能受到影响的数据或权限。

请勿在公开 Issue、Pull Request、Discussion 或聊天中提交以下内容：

- AppID 与 AppSecret 的组合；
- access token、Cookie、DPAPI 凭据文件；
- 公众号后台截图中的私人账号信息；
- 本机绝对路径、素材原文或未脱敏日志。

如果 AppSecret 或 access token 已经公开，请立即在微信公众平台重置密钥，并删除或编辑含密钥的公开内容。仅删除 Git 历史中的可见字符串并不能使已泄露密钥重新安全。

## 项目安全边界

- 只允许调用 `api.weixin.qq.com` 官方服务端 API。
- 只允许素材校验、素材上传、草稿创建及 `draft/get` 回读验收。
- 不接受正式发表、群发、删除草稿或删除永久素材的实现。
- 不接受读取浏览器 Cookie、控制微信后台或浏览器/RPA 兜底。
- AppSecret 只能由用户在本机交互式终端录入，并使用当前 Windows 用户的 DPAPI 保护。

永久素材上传会消耗微信账号配额，网络出口 IP 也会影响白名单。两者属于微信账号侧资源，运行前请确认目标账号与素材无误。
